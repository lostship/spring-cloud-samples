server:
  port: 8001

spring:
  application:
    name: cloud-provider-payment
  cloud:
    consul:
      host: 192.168.1.7
      port: 8500
      discovery:
        service-name: ${spring.application.name}

logging:
  level:
    root: info